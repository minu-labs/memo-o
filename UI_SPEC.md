# MemoO UI 스펙 (목업 기반)

이 문서는 캔버스에서 만든 목업(메인/녹음 목록/녹음 상세 3개 화면)을 실제 PySide6 구현에 참고할 수 있도록 정리한 것입니다.
프로젝트 폴더에 `UI_SPEC.md`로 저장하고, `CLAUDE.md`와 함께 두면 Claude Code가 GUI 작업 시 참고합니다.

## 공통 스타일

| 항목 | 값 |
| --- | --- |
| 창 크기 | 440 x 640 px (고정 또는 최소 크기로 사용) |
| 폰트 | Segoe UI (한글은 맑은 고딕) |
| 배경색 | #F3F3F3 |
| 카드 배경 | #FFFFFF, 테두리 #E5E5E5, radius 8px |
| 강조색(파란) | #0078D4 |
| 녹음 버튼(빨강) | #D13438 |
| 완료 상태 배지 | 배경 #E6F4EA, 텍스트 #1A7F37 |
| 진행중 상태 배지 | 배경 #FFF4CE, 텍스트 #8A6D00 |
| 타이틀바 | 높이 32px, 배경 #202020, 텍스트 흰색, 우측에 최소화/최대화/닫기 버튼 |

## 화면 1. 메인 (녹음)

- 중앙 상단에 지름 96px의 원형 녹음 버튼 (배경색 항상 빨강 #D13438)
  - 대기 상태: 버튼 안에 흰색 원(지름 36px) 아이콘
  - 녹음 중 상태: 버튼 안에 흰색 사각형(36→22px) 아이콘으로 전환 (버튼 색은 그대로 빨강 유지)
  - 클릭 시 대기 ↔ 녹음중 토글
- 버튼 아래 라벨: "녹음 시작" / "녹음 중지" (상태에 따라 전환)
- 상태/시간 표시: "상태: 대기 중 / 녹음 중..." · "시간: 00:00:00" (녹음 중엔 타이머 증가)
- 마이크 선택 드롭다운 (기본 마이크 표시)
- "최근 녹음" 카드: 최근 3건 리스트 (날짜 + 제목 + "열기" 링크) → 클릭 시 상세 화면
- 하단에 "녹음 목록 전체보기" 링크 → 목록 화면으로 이동

## 화면 2. 녹음 목록

- 상단: 검색 입력창 + "+ 새 녹음" 버튼(파란색, 클릭 시 메인 화면으로)
- 리스트: 카드형 스택 (테이블 아님, 좁은 창 폭에 맞춤)
  - 각 항목: 제목(굵게) / 날짜 · 길이(회색 작은 글씨) / 우측에 상태 배지("완료" 초록, "변환 중" 노랑)
  - 항목 클릭 시 상세 화면으로 이동
- 하단: 페이지네이션 ("< 이전  1/3  다음 >")

## 화면 3. 녹음 상세

- 상단 헤더: 뒤로가기(←) + 제목 · 날짜, 클릭 시 목록으로 복귀
- 재생 컨트롤: 이전/재생(파란 원형)/다음 버튼 + 진행바 + 시간(현재/전체)
- 검색 입력창: 회의 내용(텍스트) 검색
- 스크롤 가능한 트랜스크립트 영역: 타임스탬프(회색) + 텍스트 줄 단위 표시
- 하단 액션 버튼 2개 (가로로 나란히, 각각 flex:1로 폭 균등 분배)
  - "내보내기": 파란색 배경, 흰 텍스트
  - "삭제": 연한 빨강 배경(#FDECEC), 빨간 텍스트(#D13438)

## 화면 전환 흐름

```
메인 ──(녹음 목록 전체보기)──> 녹음 목록 ──(항목 클릭)──> 녹음 상세
  ^                                │                         │
  └──────────(뒤로가기)────────────┴─────────────────────────┘
메인 ──(최근 녹음 항목 "열기")──> 녹음 상세 (직접 이동)
녹음 목록 ──(+ 새 녹음)──> 메인
```

## 참고

- 실제 개발 시 폰트/픽셀 단위는 PySide6의 스타일시트(QSS)로 그대로 옮길 수 있음 (예: `border-radius: 8px; background-color: #FFFFFF;`)
- 색상 값은 위 표를 그대로 재사용
- 녹음 버튼은 QPushButton + QSS로 원형 구현 (border-radius: 48px, 지름과 동일하게 width/height 고정)
