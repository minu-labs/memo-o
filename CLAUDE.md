# 프로젝트: MemoO

## 개요
Windows용 오프라인 녹음 + 텍스트 변환(STT) 프로그램.
로컬에서 완전히 동작하며, 배포는 minulog.com을 통해 무료로 진행.
회의뿐 아니라 아이디어 메모, 강의, 상담 등 범용적인 음성 녹음/기록에 쓸 수 있도록 문구를 일반적으로 유지한다.

## 네이밍
- **표기명**: MemoO
- **저장소명**: memo-o (GitHub)
- exe 파일명, 패키지명 등은 memo-o 표기로 통일

## 대상 플랫폼
- **Windows 전용** (Mac 미지원, 향후 고려하지 않음)
- 인터넷 연결 없이도 동작해야 함 (STT는 로컬 모델 사용)

## UI/UX
- 상세 스펙은 `UI_SPEC.md` 참고 (색상, 레이아웃, 화면 전환 흐름 정의됨)
- 핵심 요약:
  - 창 크기: 440 x 640px (작고 심플한 유틸리티 앱 형태)
  - 녹음 버튼: 지름 96px 원형, 항상 빨간색(#D13438) — 대기 시 흰 원, 녹음 중엔 흰 사각형 아이콘으로 전환
  - 화면 3개: 메인(녹음) / 녹음 목록 / 녹음 상세
  - 목록은 좁은 폭에 맞춰 테이블이 아닌 카드형 스택 리스트로 구성
  - 예시 텍스트는 "회의"에 국한하지 않고 "아이디어 메모", "강의 노트" 등 범용 표현 사용

## 기술 스택
- **언어**: Python
- **GUI**: PySide6
- **녹음**: sounddevice (마이크 입력 → wav 파일 저장)
- **STT**: faster-whisper (로컬 모델, 한국어)
  - 모델 크기: small 기준으로 시작 (PC 사양에 따라 medium 고려)
  - GPU(NVIDIA) 있으면 CUDA 가속 활용
- **DB**: SQLite (녹음 메타데이터: 날짜, 제목, 파일 경로, 변환 텍스트)
- **패키징**: PyInstaller → 단일 .exe (memo-o.exe)
- **설치 프로그램**: Inno Setup 또는 NSIS — 설치 시 바탕화면 아이콘 자동 생성

## 명시적으로 사용하지 않는 것
- FastAPI, Nginx, 웹 서버 일체
- Oracle Cloud, Vercel 등 클라우드 배포 (앱 자체는 클라우드 불필요)
- MySQL (SQLite로 충분)
- Tauri/Electron, 브라우저 기반 UI
- Mac/Linux 대응 (Windows 전용)

## 배포 및 수익화 방향
- 앱 자체는 **완전 무료** 배포
- exe/설치 파일은 **GitHub Releases**(memo-o 저장소)에 업로드
- **minulog.com**에 다운로드 페이지 별도 제작 (Vue3 + Vercel, 기존 인프라 재사용)
- 앱과 다운로드 페이지 모두 광고 없음 — 완전 로컬 실행 유지

## 법적/개인정보 고려사항
- 통신비밀보호법상 본인이 참여한 녹음은 상대방 동의 없이도 적법하나, 본인이 참여하지 않은 대화를 몰래 녹음하는 것은 불법이므로 사용자 책임임을 앱/다운로드 페이지에 고지
- 음성 데이터가 로컬에서만 처리되어 외부로 전송되지 않는다는 점을 명시 (개인정보 리스크 완화 요인이자 마케팅 포인트)
- 다운로드 페이지에 개인정보처리방침 게시
- faster-whisper 등 오픈소스 라이브러리 라이선스(MIT) 고지

## 개발 순서
1. 파이썬 가상환경(venv) 세팅, 필요 패키지 설치
2. 마이크 녹음 기능 구현 (sounddevice → wav)
3. faster-whisper STT 연동
4. PySide6 GUI 구성 — `UI_SPEC.md` 기준으로 3개 화면(메인/목록/상세) 구현
5. SQLite로 녹음 메타데이터 저장/조회
6. PyInstaller exe 패키징 (memo-o.exe)
7. Inno Setup/NSIS 설치 프로그램 제작 (바탕화면 아이콘 포함)
8. minulog.com 다운로드 페이지 제작 및 배포

각 단계는 순차적으로 진행하고, 단계마다 실행 가능한 상태로 만들어 테스트한 뒤 다음 단계로 넘어갈 것.

## 코드 스타일 / 기타 참고
- (필요시 여기에 코딩 컨벤션, 폴더 구조, 네이밍 규칙 등을 추가)
- (사용자가 선호하는 언어: 코드 주석/커밋 메시지는 한국어 또는 영어 — 결정 후 명시)

## 향후 고려 사항 (지금은 범위 밖)
- 화자 구분(diarization) — pyannote-audio 추가 검토
- 회의록 자동 요약(LLM 연동)
- Obsidian 마크다운 export 연동
- Mac 버전 (필요 시 별도 빌드 환경에서 재검토)
